# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **项目状态(2026-08)**:v0.4.0 —— Rust + WASM + Tauri 全栈实现。4 个 PRD(recovery / changes / reports / connectors)+ code-repo 源(PRD-006)+ identity resolution 全部落地。架构与设计决策见下方「活跃栈」章节 + `doc/`。

## Project Overview

SRE 云原生巡检图谱平台 — cloud-native resource inspection graph platform。核心:**Tauri 2.x 桌面应用** + **Rust engine + WASM connectors** + **WIT/Arrow 契约**。覆盖 4 层资源图模型 + 恢复动作引擎(PRD-001)+ 变更追踪(PRD-002)+ 自检报告(PRD-003)+ 数据源 connector(PRD-004)+ 代码仓源(PRD-006)。

---

## 活跃栈(Rust + WASM + Tauri,新代码以此为准)

### 战略决策(详见 doc/15-17)

- **canonical Fact 唯一契约**:所有 connector 产出统一 7 字段 Fact,所有下游只认它 + 一个 `fact_schema()` Arrow Schema(平台可扩展性的支点)
- **Tauri 2.x 桌面优先**,不走 SaaS Web 默认(对照 k9s / Lens;数据不出本机)
- **三层数据契约**:WIT(WASM 边界)+ Tauri commands(UI↔Rust 进程内 IPC)+ Arrow/SQLite/Parquet(存储)。**无 REST / Flight 跨进程 RPC**(headless engine-cli 才用)
- WASI ABI:**默认 p2**(`wasm32-wasip2` Tier 2 stable),p3(async-native)opt-in 留口子,详见 doc/16 §4.x

### 顶层结构

```
graph_data/
├── engine/             # Rust workspace — engine 内核 + CLI(10 crate)
│   └── crates/
│       ├── engine-core/        # ✅ canonical Fact + Arrow Schema(7 列)+ FactBatch→RecordBatch + GraphResponse(facts_to_graph)
│       ├── engine-wasm/        # ✅ wasmtime host + 多 connector 编排 + capability 注入(含 http-client)
│       ├── engine-bindings/    # ✅ wasmtime bindgen 出来的 host glue
│       ├── engine-storage/     # ✅ Storage trait + SqliteStorage(raw Fact 落库,Phase 2.1)
│       ├── engine-cli/         # ✅ headless binary(tick 子命令)
│       ├── engine-testkit/     # 骨架
│       ├── engine-identity/    # ✅ Identity Resolver v0(resolve/diff/topology_to_graph + Phase 2.7 health_merge)
│       ├── engine-recovery/    # ✅ Phase 3.1-3.3:action_defs + dry_run + execution/approval/rollback + verifiers/auto-rollback + chains
│       ├── engine-changes/     # ✅ Phase 3.4-3.5:ChangeEvent + record_change + propagation + frequency + alert 关联 + yaml_diff + correlated_changes + suggest_for_change 桥
│       └── engine-reports/     # ✅ Phase 4.1-4.3:3 模板 + 订阅/调度/SMTP(PRD-003 复刻)
├── desktop/            # Tauri 2.x 桌面(React 18 + AntD + Cytoscape)
│   └── src-tauri/src/
│       ├── lib.rs              # ✅ 启动 WasmRuntime + SqliteStorage → .manage(AppState)
│       └── commands/           # ✅ wasm.rs(list_connectors / sync_all_now)+ topology.rs(get_topology / get_graph)+ system.rs
├── modules/            # 独立 WASM workspace(target 隔离,wasm32-wasip2)
│   ├── manifest.toml           # 引擎启动读的模块清单
│   ├── sdk/                    # guest 端 WIT bindings
│   └── connectors/
│       ├── hello-world/        # ✅ scaffolding(WIT 端到端,真集群阶段禁用)
│       ├── k8s-mini/           # ✅ scaffolding(多 connector 编排验证,禁用)
│       ├── prometheus/         # ✅ PRD-004 #2(http-client GET /api/v1/query → metric Fact)
│       ├── k8s/                # ✅ PRD-004 #1(真实 K8s API via kubectl proxy → topology Fact)
│       ├── jaeger/             # ✅ PRD-004 #3(trace,CHILD_OF 聚合 → CALLS 边;Phase 7.1)
│       ├── k8s-events/         # ✅ PRD-004 #5(K8s Events → ChangeEvent;首个 change-fact 生产者;Phase 7.2)
│       ├── flagd/              # ✅ PRD-004 #4(flagd POST ResolveAll → ChangeEvent + scenario 富化;Phase 7.3)
│       └── code-repo/          # ✅ PRD-006 代码仓(本地 fs 扫描 via fs-read → CodeRepo/Library + DEPENDS_ON/BUILDS;Phase 8.1)
├── specs/wit/          # ✅ 中立契约:host / connector / rule / handler 4 个 world
└── doc/                # 23 份文档(00 导航 + 01-13 设计 + 15/17 架构 + blog/7 篇)
```

### Phase 1 进展

| 增量 | 内容 | 状态 |
|---|---|---|
| A-B | WIT + toolchain:host-capabilities world + cargo aliases + cargo-component metadata | ✅ |
| 第一刀 | host wasmtime 真加载 hello_world.wasm 端到端跑通 | ✅ |
| C+D | WasmRuntime 多 connector 编排 + canonical Fact + Arrow RecordBatch + engine-cli tick | ✅ |
| E | k8s-mini 第二条 WASM connector + multi-connector 编排验证 | ✅ |
| F | Tauri ↔ engine-wasm 桥接(`list_connectors` + `sync_all_now` invoke) | ✅ |
| **G** | **http-client capability host 实装**(reqwest GET + capability allow-list) | ✅ |
| 收官 | 最小拓扑视图 + GUI verifier + Blog Part 1 + Option A polish | ✅ |

### Phase 2 进展

| 增量 | 内容 | 状态 |
|---|---|---|
| 2.1 | engine-storage SqliteStorage(raw Fact 落库 + `latest_topology_facts` 去重)| ✅ |
| 2.2 | Tauri `AppState { runtime, storage, proxy }`,`sync_all_now` sync 后 upsert 到 SQLite | ✅ |
| 2.3 | `get_topology` command + 前端启动从 SQLite 恢复拓扑(重启不 sync 也能渲染)| ✅ |
| 2.4 | GraphResponse DTO(engine-core `facts_to_graph` + Tauri `get_graph` + 前端改吃 `{nodes,edges,summary}`,对齐 `GraphResponse`)| ✅ |
| 2.5 | engine-identity ChangeSet resolver v0(`resolve`/`diff`/`topology_to_graph`)+ materialized `topology_nodes`/`topology_edges` 表 | ✅ |
| 2.6 | Prometheus connector WASM 化(首个消费 `http-client` capability:GET `/api/v1/query` → 解析 Prom JSON → metric Fact)| ✅ |
| 2.6b | 真实 K8s API connector WASM 化(`modules/connectors/k8s` via 本地 kubectl proxy;Deployment/Pod/Service/Node → topology Fact + owner 链 + health;真集群 otel-demo 验证)| ✅ |

| 2.7 | metric->topology health 合并(engine-identity `health_merge`,doc/11 §4.3 field-ownership v0:最严重胜出)+ desktop 托管 kubectl proxy(`commands/proxy.rs`)+ per-connector manifest config(`select_config` + `enabled`)+ Tauri 真集群拓扑(shape=type/fill=health/border=risk 真色);真集群 headless 验证:engine-cli tick k8s 经 manifest api_base 拉 71 fact | ✅ |

### Phase 3 进展

> PRD-001(recovery)/ PRD-002(changes)复刻。审批语义已定(doc/15):**桌面单机确认门**(保留 risk->status,丢 `approver_team`/24h TTL/多人 approve-reject)。

| 增量 | 内容 | 状态 |
|---|---|---|
| 3.1 | engine-recovery `action_defs`(8 action 元数据 + `propagation` 规则 + rule/change 推荐)+ `cascade::dry_run`(BFS blast radius,I/O-free 吃 `&Topology`)+ contract test(移植 `TestActionDefs`/`TestDryRun`,合成 9 节点 11 边拓扑) | ✅ |
| 3.2 | engine-recovery `execution` 管线(validate->dry_run->approval gate->mock handler)+ `approval`(单机确认门 confirm/cancel)+ rollback(skip re-approval)+ 8 mock handler + SQLite `recovery_executions` 表 | ✅ |
| 3.3 | engine-recovery `verifiers`(8,读 mutated twin attrs)+ auto-rollback(verify_failed -> 反向 exec,marker 防递归)+ `chains`(3 模板 × 3 on_failure:Stop/RollbackAll/Continue)+ **mutable twin**(handler `&mut ResolvedNode` 写回 attributes_json) | ✅ |
| 3.4 | engine-changes `ChangeEvent` 模型(18 字段)+ `record_change`(校验/ID/propagated_to/severity/commit_sha 回填)+ propagation 反向 BFS(`derive_propagation`/`find_propagation_path`/`find_descendants`,I/O-free 吃 `&Topology`)+ 内存 `ChangeRegistry` CRUD | ✅ |
| 3.5 | engine-changes frequency(过频 low->medium + `[过频变更]` tag)+ alert 关联(`correlate_alerts`/`correlate_changes_for_alert`)+ yaml_diff(`compute_yaml_diff` + `summarize_diff`,剥 10 噪声字段)+ `correlated_changes`(direct/propagated + 时间窗)+ `get_recovery_suggestion` 桥(接 `engine_recovery::suggest_for_change`,direct/propagated/unresolved)+ `record_change` 接入频率检查 + `serialize` | ✅ |
| 3.6 | Tauri commands(recovery/change_events/alerts ~31 条)+ AntD 视图(全量移植 Recovery/Changes)+ SQLite 持久化(change_events/recovery_chains/alert_events 3 新表 + 启动 from_* 载入)+ 接 sync 管线(拓扑消费 only) | ✅ |
| 3.7 | k8s connector 边富化(`topology-edge` fact:SCHEDULED_ON/ROUTES_TO/USES + ConfigMap/Secret node;`facts_to_graph` 分流 node/edge;`latest_topology_facts` SQL 含 edge;真集群 47 edge:22 SCHEDULED_ON+22 ROUTES_TO+3 USES)| ✅ |

| 3.8 | k8s connector Application/Component/Middleware 层(从 deploy 派生:normalize_component_name + detect_middleware + is_infra;CONTAINS[app->comp 派生]/DEPLOYED_AS[comp/mw->deploy]/BELONGS_TO[deploy->comp,comp->app 反向] edge;真集群 1 app+16 comp+1 Redis+1 Kafka + 97 edge[32 BELONGS_TO+18 DEPLOYED_AS+22 SCHEDULED_ON+22 ROUTES_TO+3 USES])| ✅ |

### 关键 crate 入口(稳定导航)

> 本节只列**模块清单 + 一句话职责**,回答「东西在哪」。演进史、行为细节、计数一律只在上方 Phase 日志 —— **单一事实源,本节不堆叠会漂移的事实**(模块增删时更新此处即可)。

- **engine-core**(`engine/crates/engine-core/src/`)—— canonical 契约唯一定义处:
  - `fact.rs` — `Fact`(7 字段)+ `fact_schema()`(Arrow Schema)+ `FactBatch`(→ RecordBatch)
  - `graph.rs` — `facts_to_graph(&[Fact]) → GraphResponse`(node/edge 去重 + 派生 CONTAINS + 悬空过滤)+ `summarize`(risk/health 桶统计唯一入口)
  - `types.rs` — resource_type / edge_type 中央注册表(const + `is_known` 回归守卫)
- **engine-identity** —— 拓扑身份 + 图遍历(全 I/O-free):
  - `topology.rs` — `Topology` + `resolve(&[Fact])` + `topology_to_graph`
  - `correlation.rs` — `rewrite_by_correlation_key`(多源合并 pre-rewrite pass)
  - `changeset.rs` — `ChangeSet` + `diff`(增量同步基准)
  - `health_merge.rs` — metric → topology health 合并(worst-severity)
  - `views.rs` — `subgraph` 图遍历原语(6 巡检视图共用)
- **engine-wasm** —— wasmtime host + capability 注入:
  - `runtime.rs` — `WasmConnector` / `WasmHandler` + host trait impls(logging/clock/http-client/fs-read)
  - `http_host.rs` / `fs_host.rs` — capability 纯函数实装(与 WIT 绑定解耦,可单测)
  - `handler_executor.rs` — `WasmHandlerExecutor`(impl `HandlerExecutor`,real 模式真改集群)
  - `multi.rs` — `WasmRuntime` 多 connector 编排 + `SyncSummary`(per-connector 计数/错误/耗时)
  - `lib.rs` — manifest schema(`ModuleManifest`)+ `WasiVersion`;`examples/bench_load.rs` 实例化耗时基准
- **engine-storage** —— `Storage` trait + 双后端:
  - `sqlite.rs` — latest 真相源 + materialized 拓扑 + 各领域表(facts/recovery/change/alert/report)
  - `parquet_store.rs` — append-only 归档(按 date/source 分区)
  - `examples/` — `dump_topology` / `inspect_views` / `archive_facts`(GUI-less 验证)+ `bench_core`(resolve/diff/get_graph 计时)
- **engine-recovery** —— PRD-001 恢复引擎:`action_defs.rs`(8 action + 传播规则)· `cascade.rs`(dry-run BFS 爆炸半径)· `execution.rs`(管线/审批/回滚)· `verifiers.rs`(执行后验证 + 自动回滚)· `chains.rs`(多步链)· `handlers.rs`(mock twin handler)· `executor.rs`(`HandlerExecutor` trait)· `models.rs`
- **engine-changes** —— PRD-002 变更追踪:`models.rs`(ChangeEvent)· `propagation.rs`(反向 BFS + 8 边白名单)· `event_service.rs`(registry + record_change)· `yaml_diff.rs`(噪声剥离 + 确定性 diff)· `frequency.rs`(过频检测)· `alerts.rs` · `alert_correlation.rs`(时间窗关联)· `watch.rs`(poll-diff detect_changes)· `alert_aggregation.rs`(告警聚合视图)· `iso.rs`(ISO8601 工具)
- **engine-reports** —— PRD-003 自检报告:`gatherers.rs` / `cluster_gatherers.rs` / `incident_gatherers.rs`(I/O-free 采集器)· `health_score.rs` · `generator.rs`(Tera 渲染)· `templates/`(3 模板)· `subscription.rs`(订阅 CRUD)· `scheduler.rs`(no-catch-up 调度)· `email.rs`(EmailSender trait + InMemory 回退)· `runner.rs` · `models.rs`
- **engine-cli**(`engine-cli/src/main.rs`)—— headless binary:`tick` 单次 / `tick --loop --interval=N` 持续
- **desktop/src-tauri** —— Tauri 薄命令层:`lib.rs`(AppState + 启动序列)+ `commands/`(wasm / topology / proxy / recovery / change_events / alerts / reports / reports_scheduler / views / connectors / system)+ `sync_loop.rs`(后台 sync)+ `scheduler.rs`(报告调度 loop)+ `email_smtp.rs`(SMTP sender)
- **desktop/src**(前端)—— AntD + react-router,10 页:`pages/`(Topology / Recovery / Changes / Reports / Connectors + node-impact / config-impact / access-link / image-risk / alert-aggregation)+ `components/`(Graph / Recovery / Reports / Views / Connectors / Layout)+ `api/client.ts`(invoke 封装 + 全命令 TS 类型)

### 常用命令

```bash
# Engine(engine/ 是独立 workspace,命令在 engine/ 下跑)
cd engine && cargo build --workspace
cd engine && cargo test --workspace
cd engine && cargo clippy --workspace --all-targets -- -D warnings
cargo run --release -p engine-cli -- tick              # 加载 manifest + 跑一次 sync_all
cargo run --release -p engine-cli -- tick --loop --interval=30   # 持续 sync(Ctrl-C 退)

# WASM modules(独立 workspace,target 隔离)
cd modules && cargo wasi-build               # 出 wasm32-wasip2 产物
MODULES_ROOT=/abs/path engine-cli tick       # 用指定 manifest 根跑

# Desktop(Tauri)
cd desktop && npm run tauri dev              # webview + Rust backend HMR
cd desktop && npm run tauri build            # 出 .app/.AppImage/.msi
cd desktop && npm test                       # 前端 vitest
SRE_GRAPH_DB_PATH=/tmp/x.sqlite npm run tauri dev   # 指定 SQLite 路径(默认 app data dir)
```

> 顶层 `Makefile` 已落(对齐 doc/16 §10):`make help` 列全 target;`make test-all` / `check-all` / `build-all` 聚合;`engine-*` / `modules-*`(`cargo wasi-build` alias)/ `desktop-*` / `ref-*`(legacy oracle)分组;`engine-inspect-views DB=...` 跑 6 视图 headless 验证。

### 三层数据契约(写新代码前对照 doc/15)

| 层 | 协议 | 边界 | 现状 |
|---|---|---|---|
| A | **WIT**(Component Model) | WASM ↔ host | ✅ `specs/wit/` 4 world;host 用 `wasmtime::component::bindgen!`,guest 用 `wit_bindgen::generate!` |
| B | **Tauri commands**(JSON IPC) | webview ↔ Rust | ✅ `commands/wasm.rs` + `commands/topology.rs`(get_topology / get_graph→`GraphResponse`);Phase 2+ 继续拆 `commands/{recovery,...}.rs` |
| C | **Arrow RecordBatch** + Parquet + SQLite | engine 内部 | 🔨 engine-core Arrow Schema ✅;engine-storage SQLite raw Fact backend ✅(Phase 2.1)+ materialized `topology_nodes`/`topology_edges` 表 ✅(Phase 2.5);Parquet 归档待后续 |

**反模式(不做)**:Tauri 里又起 HTTP server(用 invoke 直接 IPC);desktop/ 写业务逻辑(逻辑在 engine-core,Tauri command 是薄包装);WASM 模块直接 syscall(host 注入 capability,deny by default)。

### Capability 设计(http-client 实例,Phase 1 G)

- **deny by default**:`manifest.toml` 每个模块 `capabilities = [...]` 显式申明;host 调用时 allow-list 查表,缺则返 `HostHttpError::Unauthorized`
- **call-time 拒绝(非 link-time)**:共享 Linker,`http_get` 每次查 `HashSet<String>`,简单 + 后续加 URL allow-list 平滑
- **host 类型与 WIT binding 解耦**:`HostHttpResponse` / `HostHttpError` 在 `http_host.rs` 定义,可单测;`runtime.rs::HttpClientHost::get` 是薄适配做类型平移
- **状态码映射**:401/403 → `Unauthorized`;404 → `NotFound`;timeout → `Timeout`;其它(含 5xx)透状态码 + body 给 guest 自决
- **首个消费方(Phase 2.6)**:`modules/connectors/prometheus` —— `bindings::sre::inspection::http_client::get(url, [])` 发 GET,manifest `capabilities=["logging","clock","http-client"]`。缺 `http-client` 时 host 返 `Unauthorized`,guest 整轮 0 fact + error note(`tests/prometheus_http_e2e.rs` 两个 case:mock Prom server 放行 + deny-by-default 拒绝,均覆盖)

### Capability 设计(fs-read 实例,Phase 8.1)

- **首个非网络 capability**:`fs-read`(read-file/read-dir,read-only)。镜像 http-client 模板(WIT interface + `fs_host.rs` 纯函数 + `runtime.rs::FsReadHost for State` + `ModuleManifest.fs_roots` 透传)。`add_to_linker` 自动纳入新 import —— 加 capability 只改 WIT + 给 State 加 impl。
- **path-root allow-list 第一天强制**(区别于 http-client 的 URL allow-list 仍欠的 TODO):`fs_host::resolve_and_check` = capability 查 + `std::fs::canonicalize`(解析 `..` + 符号链接)+ 请求路径 `starts_with` 某 canonical root(组件级,非字符串前缀)。防目录穿越 `../../etc/passwd` + 符号链接逃逸。**有 cap 无 roots = 无访问**(`fs_roots` 空 → PermissionDenied)。read-only:只 read-file/read-dir,无 write/create/delete(区别于 raw WASI preopens 给完整读写删)。
- **handler-world 不 import fs-read** → `FsReadHost` 只 impl 一次(connector-world);不像 http-client 要为两 world 各 impl 一份。
- **首个消费方(Phase 8.1)**:`modules/connectors/code-repo` —— `bindings::sre::inspection::fs_read::read_file/read_dir` 扫本地仓库,manifest `capabilities=["logging","clock","fs-read"]` + `fs_roots=["<abs dir>"]`。缺 `fs-read` 时 host 返 `PermissionDenied`,guest 0 fact + error note(`tests/code_repo_e2e.rs` 真 fs fixture 放行 + deny-by-default 两 case)

### 待办

- [x] Phase 1 收官:最小拓扑视图(打开 app 看到 mock 拓扑图)+ Blog Part 1 + GUI verifier + Option A 首屏 polish
- [x] Phase 2 第一刀:SQLite-backed mock topology persistence(`sync_all_now` 写入 SQLite,重启后 `get_topology` 从库恢复;GUI X11 验证通过)
- [x] Phase 2.4:GraphResponse DTO(engine-core `facts_to_graph` + Tauri `get_graph` + 前端改吃 `{nodes,edges,summary}`;boot 从 SQLite 恢复并成图渲染,GUI X11 验证通过)
- [x] Phase 2.5:Identity Resolver v0(engine-identity `resolve`/`diff`/`topology_to_graph` + engine-storage materialized `topology_nodes`/`topology_edges` + Tauri sync 维护 + `get_graph` 改读 materialized;headless `dump_topology` 验证 + 全栈 cargo/vitest 绿)
- [x] Phase 2.6:Prometheus connector WASM 化(`modules/connectors/prometheus` 首个消费 http-client capability;GET `/api/v1/query` → Prom JSON → metric Fact;mock-server e2e + deny-by-default 验证)
- [x] Phase 2.6b:真实 K8s API connector WASM 化(`modules/connectors/k8s` 经本地 `kubectl proxy` 明文 HTTP 拉 API;纯 mapper 把 Deployment/ReplicaSet/Pod/Service/Node 映射成 topology Fact —— owner 链 Pod→RS→Deploy、health 由 phase/ready 推导、parent 层级;真集群 otel-demo 验证:71 fact / GraphResponse nodes=71 edges=70 health{critical:1,warning:4})。**架构**:WASM 只用 http-client,TLS+认证留 kubectl proxy,不碰凭据、不加 capability
- [x] Phase 2.7(可选):metric→topology health 合并(需 Identity Resolver field-ownership,见 doc/11 §4.3)+ desktop 托管 kubectl proxy 生命周期 + Tauri 视图迁真集群拓扑。✅ 完成:health_merge/storage/proxy/select_config/vitest 单测全绿 + 真集群 headless(engine-cli tick k8s 经 manifest api_base 拉 71 fact;prometheus OOM 0 fact 符合预期,merge no-op)
- [x] Phase 3.1:engine-recovery `action_defs`(8 action 元数据 + propagation + rule/change 推荐)+ `cascade::dry_run`(BFS blast radius,I/O-free 吃 `&Topology`)+ contract test(移植 `TestActionDefs`/`TestDryRun`,合成 9 节点 11 边拓扑);逐字对齐 设计,clippy + 20 测试绿。**审批语义已定**(doc/15):桌面单机确认门,3.2 落地
- [x] Phase 3.2:engine-recovery `execution` 管线(execute:low 同步 / medium·high awaiting_approval)+ `approval`(单机确认门 confirm/cancel)+ rollback(skip re-approval)+ 8 mock handler + engine-storage `recovery_executions` 表;contract test 移植 risk->status + rollback 矩阵,clippy + 39+11 测试绿
- [x] Phase 3.3:engine-recovery `verifiers`(8,读 mutated twin attrs)+ auto-rollback(verify_failed -> 反向 exec,marker 防递归;无 rollback_action_id 则 warn)+ `chains`(3 模板 × 3 on_failure:Stop/RollbackAll/Continue)+ **mutable twin 架构**(handler `&mut ResolvedNode` 写回 `attributes_json`,verifier 读 mutated attrs,rollback 读 post-action 状态做正确反转);contract test 移植 verify/auto-rollback + chain on_failure 矩阵,clippy + 61 测试绿
- [x] Phase 3.4:engine-changes `ChangeEvent` 模型(18 字段)+ `record_change`(校验 type/source 400 + `ce-<12hex>` ID + target_resource_type 查拓扑 + propagated_to 反向 BFS + severity + commit_sha 回填 related_commit)+ propagation 反向 BFS(`derive_propagation`/`find_propagation_path`/`find_descendants`,I/O-free 吃 `&Topology`,8 种白名单边不含 USES_IMAGE)+ 内存 `ChangeRegistry` CRUD(add/get/list 闭区间过滤/clear);contract test 移植 `TestChangeEventModel`/`TestStore`/`TestPropagation`/`TestRecordChange`(10 节点 10 边 fixture),clippy + 29 测试绿。**偏差**:丢 图数据库 dual-write + alert 关联(3.5)、不调 frequency check(3.5)、v0 拓扑只 CONTAINS 边生产传播只沿 CONTAINS 反向
- [x] Phase 3.5:engine-changes frequency(过频 low->medium + `[过频变更:N次/Ws]` tag,`record_change` add 后调,计数含当前,回写 via `get_mut`)+ alert 关联(`correlate_alerts`/`correlate_changes_for_alert`,affected={target}∪propagated_to,窗 [changed±window])+ yaml_diff(`compute_yaml_diff` + `summarize_diff`,剥 10 噪声字段,自写确定性 block-style 发射器**不引 serde_yaml**)+ `correlated_changes`(direct/propagated + 三种时间窗 + propagation_distance)+ `get_recovery_suggestion` 桥(接 `engine_recovery::suggest_for_change`,嵌套 ActionSuggestion -> 扁平 RecoverySuggestion,direct/propagated/unresolved)+ `serialize`;contract test 移植 `TestYamlDiff`/`TestFrequencyAlert`/`TestCorrelatedQuery`/`TestRecoverySuggestion`/`TestAlertCorrelation`(8 节点 7 边 phase2 + 10 节点 sprint1 fixture),clippy + 66 测试绿。**偏差**:丢 图数据库 dual-write + `CORRELATED_WITH` 边 + `persist_correlation`(`graph_db_available` 恒 false);I/O-free 吃 `&Topology`/`&ChangeRegistry`/`&AlertRegistry`(设计 读全局 DSS `store`);丢 K8s watch + webhook(Phase 3 延后)
- [x] Phase 3.6:Tauri commands(recovery 18 / change_events 8 / alerts 5 = ~31 条)+ AntD 视图全量移植(react-router 嵌套布局 + react-query;ExecutionsView 折叠 ApprovalsView 单机确认门 confirm/cancel;RecoveryChainsView+DryRunModal;ChangeTimelineView drawer impact/alerts/suggestion;NodeDetailPanel 集成 actions+changes;TopologyView 加 onSelectNode)+ SQLite 持久化(change_events/recovery_chains/alert_events 3 新表,AppState 4 Mutex registry 启动 from_* 载入 + mutation 后 upsert)+ 接 sync 管线(拓扑消费 only:命令读 materialized topology 作 &mut twin,不写回;自动录 change event 留 k8s-watch)。mutable twin 传 owned clone;DTO 直接返 engine Serialize 类型(ChainTemplate 用 DTO);errors `[code] msg` 字符串。cargo test(clippy -D warnings;storage 14 + changes 66 + recovery 61)+ desktop tsc/vite build/vitest 14 绿;X11 启动 smoke 通(3 表 migrate + 4 registry 载入无 panic)。**偏差**:sync 拓扑消费 only(不自动录 change event)、alerts 无 live 源(仅手动 record_alert)、ApprovalsView 折叠进 ExecutionsView、真 handler 留 write-capability WIT 延后
- [x] Phase 3.7:k8s connector 边富化(`topology-edge` fact:SCHEDULED_ON[pod->node]/ROUTES_TO[svc->pod selector 匹配]/USES[pod->cm/secret,volumes+envFrom]+ ConfigMap/Secret node[只存 data_keys 不存值]);`facts_to_graph` 分流 topology-node/topology-edge(各自 dedup,edge 悬空+自环过滤,与派生 CONTAINS 合并);`latest_topology_facts` SQL `kind IN ('topology-node','topology-edge')`;engine-identity/engine-storage 零改(diff/apply 已支持任意 edge_type);engine-recovery/changes 零改(算法认全 8 白名单边,富化后自动生效)。真集群 headless:engine-cli tick k8s -> 78 node(含 4 cm+3 secret)+ 47 edge(22 SCHEDULED_ON+22 ROUTES_TO+3 USES)。**偏差**:不产 BELONGS_TO/DEPLOYED_AS/RUNS/EXPOSES(需 application/component 层);USES 只解析 volumes+envFrom(对齐 设计,不解析 env.valueFrom);Secret 只存 data_keys;edge id=`{src}->{tgt}`(K8s 同对资源无多关系不撞)。clippy+测试绿(engine-core 20/identity 17/storage 15/k8s mapper 11/changes 66/recovery 61)
- [x] Phase 3.8:k8s connector Application/Component/Middleware 层(从 deploy 派生:`normalize_component_name`[strip release prefix + 砍 service + 拆混淆名] + `detect_middleware`[valkey/redis/kafka/postgres/mysql] + `is_infra`[loadgenerator/otelcol/prometheus-server/jaeger/opensearch/grafana/kibana];`CONTAINS`[app->comp,comp.parent 派生] + `DEPLOYED_AS`[comp/mw->deploy] + `BELONGS_TO`[deploy->comp, comp->app 反向边,action BELONGS_TO forward 规则需要] edge fact;`ClusterInput` 加 `release_prefix`[默认 otel-demo];engine-core/identity/storage/changes/recovery 零改(3.7 已支持任意 edge_type + node type)。真集群 headless:engine-cli tick k8s -> 1 Application + 16 ApplicationComponent + 1 Redis + 1 Kafka + 97 edge(32 BELONGS_TO + 18 DEPLOYED_AS + 22 SCHEDULED_ON + 22 ROUTES_TO + 3 USES)。**偏差**:产 BELONGS_TO 反向边(设计 k8s_mapper 不产,action BELONGS_TO forward 需要);Application parent=ns(设计 无 parent);component 名=normalize(deploy_name)不用 labels component;infra deploy 不挂 application。clippy+测试绿(k8s mapper 15[+4 3.8]/engine 回归 全绿)
- [x] 3.9a async 架构重构(host tokio,为 real handler 铺路):engine-recovery HandlerExecutor `#[async_trait]` async + execution/chains 全 async + `async_recursion`(run_handler↔verify 递归)+ tests `#[tokio::test]`;desktop AppState 5 Mutex 改 `tokio::sync::Mutex`(proxy 保持 `std::sync::Mutex`)+ `lock().await` + sync commands 改 `async`。61 测试 + clippy 绿。解决 3.9a-3b1 async 障碍(HandlerExecutor sync vs real handler async + AppState `std::sync::Mutex` Guard not Send)。real handler 接入(3.9a-3b2 WasmHandler)无 async 障碍
- [x] Phase 3.9a-3b2:real handler 接入收官。**WasmHandlerExecutor 从 desktop 移到 engine-wasm**(`engine/crates/engine-wasm/src/handler_executor.rs`;engine-wasm 拥有 `WasmHandler`,executor 是其自然消费者,可在 `tests/` 加 mock 单测 + proxy-gated 真集群集成测试覆盖完整胶水;desktop 仅构造 + env 开关)。**修 3 个 latent bug**(原 desktop 版 real mode 从未真跑通):① **attrs 合并** -- k8s-handler 只返 `{desired_replicas,available_replicas}` 等动作生效字段,host 读 target 现有 attrs(`cluster`/`name`/`replicas_desired`/...)overlay WASM 返字段后返**合并全量**(否则 `run_handler` 整体替换 attributes_json 擦掉 connector 字段);② **result 合成 verifier 字段** -- 据 action_id 从合并 attrs 合成 verifier 期望字段(scale→`new_replicas`/restart_pod→`new_restart_count`/restart_service→`endpoints_refresh_count`/refresh_secret→`new_version`/rollback_deployment→`new_revision`;否则 WASM 模式 6 verifier 全 fail 误触 auto-rollback);③ **old_replicas 名字** -- 读 target attrs 优先 `replicas_desired`(connector 名)回退 `desired_replicas`(handler 写名)再回退 3(避免首次 scale 基准错)。抽 `merge_values` + `synthesize_result_field` 纯函数单测(无 wasm)。**desktop 瘦身**:删 `commands/handler_executor.rs` + mod.rs 条目;lib.rs 改 `engine_wasm::WasmHandlerExecutor::new(h, api_base, real_mode)`;`real_mode`=`SRE_GRAPH_HANDLER_MODE`(默认 `mock` 保安全,对齐 `RECOVERY_HANDLER_MODE`;`real` 开启真改集群);`api_base`=`SRE_GRAPH_K8S_API_BASE`(默认 desktop 托管 kubectl proxy 的 `http://127.0.0.1:8001`);启动 log 明示模式。**测试**:`engine-wasm/tests/wasm_handler_executor.rs` 3 个(mock merge+synthesize 验 attrs 合并保 cluster/name + result.new_replicas==4;kill_query/mock fallback;**real proxy-gated scale +1/-1 可逆 真集群验证通过**)。engine-wasm +1 测试 crate,3/3 绿。**偏差**:kill_query/clear_cache 留 Mock(MySQL/Redis handler 延后;设计 也仅 K8s/MySQL/Redis real,K8s 优先);rollback_deployment 简化为 PATCH annotation 触发 restart(非真 rollout undo);refresh_secret 简化为 PATCH annotation(非真 data 轮换);real mode 默认关(防误改集群)。cargo test --workspace + clippy --workspace -D warnings + desktop check/clippy + 前端 tsc 全绿
- [ ] Phase 3 延后:webhook(image_pushed;桌面架构冲突,可能跳过;手动录入口已就绪)。注:RUNS/EXPOSES 边已落地(见 Phase 3.9 条目);k8s-watch 已以 poll-diff 落地(见 PRD-003/002 后续条目),非 streaming watch;real handler 接入已收官(见 3.9a-3b2)
- [x] Phase 4.1:engine-reports application_health 模板(ReportTask + ReportStore + health_score[compute_health_score 公式 100-critical×10-warning×3-fault_pod×2,rating >=80 健康/60-79 健康警告/40-59 风险中/0-39 风险高]+ 5 采集器[health_score/seven_views/risk_list/recommended_actions/historical_trends,I/O-free 吃 &Topology/&ChangeRegistry/&ExecutionRegistry]+ Tera 模板+ generator[按 modules 采集 + Tera 渲染]+ desktop commands[generate_report_cmd/list_reports/get_report]+ AppState reports Mutex)。6 测试绿 + clippy 绿。**偏差**:丢 fault injection(fault 空,fault_pod=0);丢 图数据库 持久化(内存);丢调度+SMTP(Phase 4.3);用 Tera 渲染;1 模板(application_health),cluster_overview/incident_report 留 Phase 4.2
- [x] Phase 4.2:engine-reports cluster_overview + incident_report 模板(3 模板收官)。**4.2a cluster_overview**:cluster_gatherers.rs(4 采集器 gather_cluster_health/risk_top_n/changes/recoveries,I/O-free 吃 &Topology/&ChangeRegistry/&ExecutionRegistry;cluster_id prefix 匹配过滤 None=all;rating_counts 用 ClusterRatingCounts struct[4 档 ASCII 字段,避 Tera 1.20.1 中文 key bracket 索引 bug:rating_counts["健康"] 路径切片落 UTF-8 边界崩])+ templates/cluster_overview.md(Tera;HashMap 用 {% for k,v in obj %} 迭代)+ generator 加 ClusterOverview 分支(top_n 默认 5)。**4.2b incident_report**:incident_gatherers.rs(IncidentAnchor + resolve_anchor[change_event_id;fault_id -> Err 无 fault injection]+ 3 采集器 gather_incident_summary/timeline/recoveries;受影响节点反向 BFS derive_propagation max_depth=4;timeline ±3600s 窗 ISO8601 比对;recoveries 推荐复用 suggest_for_change)+ ReportScope 加 change_event_id/fault_id 字段 + templates/incident_report.md(Tera:`or`->default、if-else 三元->预计算 kind_label、dictsort->排序 Vec<AffectedTypeCount>、`e.execution_id[:12]`->execution_id_short[Tera 无字符串切片])+ generator 加 AnchorNotFound + 3 模板穷举 match(去 `_` arm)+ generate_incident_report + desktop generate_report_cmd 加 change_event_id/fault_id 透传。12 测试绿(4 cluster_gatherers + 2 incident + 1 cluster 渲染 + 5 既有)+ cargo test --workspace 全绿 + clippy --workspace -D warnings 干净。**偏差**:丢 fault 锚点(Rust 无 fault injection,fault_id -> AnchorNotFound);丢 图数据库 持久化(内存);丢调度+SMTP(Phase 4.3);用 Tera 渲染。附带修 3.9a async 遗留(engine-storage sqlite 测试 execute 缺 executor+.await;engine-wasm wasm_handler 测试 expect_fun_call clippy)
- [x] Phase 4.3:engine-reports 调度 + SMTP + 前端 ReportsPage(PRD-003 收官,3 模板全可生成/订阅/邮件)。**4.3a 纯核心**:engine-reports subscription.rs(ReportSubscription 12 字段 + SubscriptionStatus enum + SubscriptionStore 内存 CRUD + parse_cron 5-field via `cron` crate prepend "0 " 转 6-field + validate_subscription)+ email.rs(EmailSender async trait + SentEmail + InMemoryEmailSender 默认回退捕获 + list_sent)+ scheduler.rs(check_fire 纯函数 -> FireDecision,no-catch-up 语义:next_fire<=now 且 now-next<=grace[300s] -> Fire;>grace -> MissedAdvance 推进 last_run_at 消费 stale fire;对齐 misfire_grace_time=300 + coalesce)+ runner.rs(run_subscription trigger_now + 调度循环共用:订阅 -> ReportTask -> generate_report -> email body=markdown + .md 附件 -> completed task)。**4.3b 持久化+wiring**:engine-storage report_subscriptions 表(12 列 + enum/scope/modules/recipients JSON 文本)+ upsert/get/list/delete + from_subscriptions;desktop email_smtp.rs(SmtpEmailSender via lettre 0.11 starttls/builder_dangerous + 可选 credentials + get_email_sender env[SMTP_HOST 空 -> InMemory 回退])+ scheduler.rs(60s tokio::time::interval tick,AppHandle 访问 AppState,Fire/MissedAdvance/NotDue 三分支)+ commands/reports_scheduler.rs(7 命令:subscription CRUD + trigger_subscription_now + list_sent_emails)+ AppState(+subscriptions/email_sender/scheduler_handle)+ setup spawn + from_subscriptions 载入 + RunEvent::Exit abort。**4.3c 前端**:client.ts(+10 函数 + 类型)+ components/Reports/{ReportsView,SubscriptionsView,SentEmailsView}.tsx + pages/ReportsPage.tsx;react-markdown + remark-gfm 渲染 markdown;App.tsx /reports 路由 + MainLayout 启用 reports 菜单。**偏差**:调度只在 app 开着时跑(关机漏发 >5min 不补跑,<5min 重开补跑 1 次);丢 图数据库 dual-write -> SQLite report_subscriptions;reports(ReportStore)仍内存不持久化(4.1 现状,report 跨重启历史留后续);SMTP 凭据用 env(对齐 设计,非 keychain),InMemory 回退开箱即用;丢 fault 锚点(Rust 无 fault injection);自写 cron loop(cron crate 解析 + tokio interval,非 tokio-cron-scheduler 库,为访问 AppState registry + no-catch-up 控制)。engine 29 + storage 16 测试绿 + cargo test --workspace + clippy --workspace -D warnings + desktop cargo check/clippy + 前端 tsc/vite/vitest 19 绿。contract test 移植 test_reports_sprint2_sub.py::test_trigger_now_generates_and_sends
- [x] PRD-003/002 后续:report 跨重启持久化 + k8s-watch poll-diff 自动录 change event。**report 持久化**:engine-storage report_tasks 表(12 列,enum+JSON 文本)+ upsert/get/list/delete_report + row_to_report_task + round-trip 测试;desktop setup 启动 list_reports(1000)->ReportStore::from_tasks 载入;generate_report_cmd/trigger_subscription_now/scheduler fire 三处 add 后补 upsert_report(scheduler best-effort warn);既有 list_reports/get_report 命令读 registry(loaded+synced)零改。补齐 4.1 偏差"reports 内存不持久化"。**k8s mapper 信号字段**:Deployment attrs += current_revision(annotations["deployment.kubernetes.io/revision"])+ images(spec.template.spec.containers[].image 排序去重)+ replicas_desired/replicas_ready;ConfigMap/Secret 已有 data_keys;health_status 不变(信号字段只供变更检测,防 ready<desired 误报 roll)。**detect_changes 自动录**:engine-changes watch.rs(detect_changes(current,next)->Vec<ChangeRequest> 纯函数;只看 CM/Secret/Deploy;信号字段触发 compute_yaml_diff keys=Some(signal);新资源跳过/信号未变跳过;source=k8s_api + yaml_diff + diff_summary + cluster_id;移植 TestK8sWatchConnector 7 测试)+ desktop sync_all_now diff 后 apply 前非首次 sync(first_sync_done AtomicBool,对齐 first_sync 防重启 burst)-> detect_changes -> record_change + upsert_change_event;锁纪律 record_change 锁内同步收集 events 释放后异步 upsert。**偏差**:非 streaming watch(真 watch 需 WIT stream + WASIp3,留长期);ConfigMap/Secret value-only 漏检(不存 data 值);image_pushed 跳过(webhook-only);延迟 ≤30s(poll);首次 sync 抑制。cargo test --workspace 全绿(changes 73 +7 watch / storage 17 +1 report_task / k8s mapper 16 +1)+ clippy --workspace -D warnings + desktop cargo check/clippy + X11 smoke 通过(45s 无 panic)
- [x] PRD-003/002 后续(第二轮):后台 tick_loop + report 清理 + 真集群 smoke + image_pushed 入口。**后台 tick_loop**:desktop 抽 sync_all_now body 为 run_sync(state,cfg);sync_loop.rs interval loop(env `SRE_GRAPH_SYNC_INTERVAL_SECS` 默认 30,0=禁用),跳过首次 immediate tick(前端启动 sync 作 baseline);setup spawn + RunEvent::Exit abort;让 detect_changes 真"自动"触发(不用手动点 Sync)。**report 清理**:engine-storage prune_reports(keep_n)+ clear_reports;generate/trigger/scheduler fire upsert 后 best-effort prune_reports(100);clear_reports 命令 + 前端 ReportsView"清空历史"按钮。**image_pushed 入口**:既有 record_change_event 命令已接受 image_pushed(ChangeType 强类型枚举含 4 类),前端 ChangeTimelineView record 表单已含 image_pushed 选项 -- 手动录入口已就绪,无需新命令(真 webhook 需 HTTP server,desktop 反模式,跳过)。**真集群 smoke 验证**:kubectl proxy + otel-demo,emailservice deploy rollout restart(revision 1->2),后台 tick 10s 后 detect_changes 录入 `deployment_rolled`(target=`deploy:vm-cluster:otel-demo:otel-demo-emailservice`,yaml_diff 只显 `current_revision: 1 -> 2` 信号字段精确);topology_nodes=98 真集群 sync;mapper 从真 K8s API 正确提取 current_revision/images/replicas;首次 sync 抑制生效无 burst。cargo test --workspace + clippy + desktop check/clippy + 前端 tsc/vite/vitest 21 绿全过
- [x] Phase 3.9:k8s connector RUNS/EXPOSES 边 + report trigger_source 字段。**RUNS(pod->container)**:mapper Pod loop 为 `spec.containers[]` 每个容器产 Container node(`container:{c}:{ns}:{pod}:{name}`,**无 parent** 经 RUNS 边挂 pod,不派生 CONTAINS;attrs image/ready/restart_count 从 `status.containerStatuses[]` 按 name 匹配取;health crashloop->critical / ready->normal / else warning;对照 doc/03 REL-036(Pod RUNS Container);不产 initContainers(v0)。**EXPOSES(svc->deploy)**:Service loop selector 匹配 `deploy.spec.template.metadata.labels`(pod 模板 labels,等价匹配该 deploy 全部 pod);对照 doc/02 L2(Service EXPOSES Deployment,e006);设计 未实现(仅 static CSV 种子),属增量。重构 `index_pod_labels` -> `index_labels(list,prefix,pick_fn)` + `pod_template_labels`/`deploy_template_labels` 两提取器;`PodLabelEntry`->`LabelEntry`。frontend `SHAPE_BY_TYPE` 加 `Container: "barrel"`(cytoscape 原生形状,区别 Pod ellipse)。真集群 headless:engine-cli tick k8s -> 119 node(+22 Container)+ 141 edge(22 SCHEDULED_ON+22 ROUTES_TO+22 RUNS+22 EXPOSES+3 USES+32 BELONGS_TO+18 DEPLOYED_AS)。**trigger_source**:engine-reports `TriggerSource` enum(manual_cmd/scheduled/trigger_now,snake_case serde,`#[default] ManualCmd`)+ ReportTask 字段;`run_subscription` 加 `trigger_source` 参(scheduler fire=Scheduled / trigger_subscription_now=TriggerNow / generate_report_cmd=ManualCmd);engine-storage report_tasks 加 `trigger_source` 列 + 字面量 ALTER backfill(PRAGMA table_info 查在,sqlx 0.9 `SqlSafeStr` 禁动态串故字面量)DEFAULT `'"manual_cmd"'`(JSON 文本对齐 enum 列约定)+ upsert/row_to_report_task + 迁移 round-trip 测试(旧库补列 + 默认值 + 幂等);前端 client.ts `TriggerSource` 类型 + ReportsView `source` 列 + Drawer Tag(手动/调度/立即触发 配色)。**修正误判**:旧 CLAUDE.md 注 "RUNS/EXPOSES 需 trace/ingress 层" 不确 -- 两者均可从已拉取 K8s API 数据派生(Pod spec.containers / Deploy template labels),无需额外 connector。cargo test --workspace + clippy --workspace -D warnings + desktop check/clippy + 前端 tsc/vite/vitest 绿(k8s mapper 18[+2 RUNS/EXPOSES 测试]/storage 19[+1 迁移 backfill 测试]/reports 29;engine-wasm 3 real-cluster mutation 测试 proxy-gated,proxy 关时 skip 不阻塞 gate)
- [x] Phase 5:巡检视图迁移(4 个图遍历视图,复刻 view2-5)。**核心洞察**:剩余 5 视图里 4 个共用同一原语 —— 从起点 BFS、depth 限深、edge_type 白名单过滤、有向(forward/reverse/both)、返 induced subgraph。**engine-identity `views.rs`**(新模块,~35 行 + 8 测试):`TraversalDir{Forward,Reverse,Both}` + `subgraph(&Topology, start, max_depth, allowed_edges, dir) -> Topology`(BFS 收集可达节点 ID → filter nodes + filter edges[两端命中 且 edge_type 在白名单] → induced subgraph;start 不在拓扑 -> 空)。放 engine-identity 而非 engine-changes 避依赖环(changes 已依赖 identity)。4 视图白名单常量逐字照 设计 Cypher(含 Rust 暂不产的 CONTROLLED_BY/AFFECTS/FIRED_ON/STORED_IN/DEPLOYED_IN —— 不匹配即无害,future-proof)。复用 `engine_core::{GraphResponse,summarize}` + `engine_identity::topology_to_graph` + `materialized_topology()`。**desktop `commands/views.rs`**(新模块,5 命令):`node_impact`(起点 resource_type `Node`,Reverse,4;注 设计 图数据库 label 是 `KubernetesNode` 但 Rust connector 产 `Node`)/`config_impact`(Secret|ConfigMap,Reverse,4)/`access_link`(Application,Both,5)/`image_risk`(起点 `ContainerImage`,Reverse,4;connector 尚未产 image 节点 -> 空图)+ `list_resources_by_types`(选择器数据源)+ lib.rs generate_handler 注册。**前端**:`InspectionGraphView` 通用骨架(起点 Select[按 type 拉候选]+ depth Slider + 复用 `<TopologyView graph={sub}/>`)+ 4 薄 page(node/config/access/image)+ App.tsx 4 路由 + MainLayout 4 菜单(AimOutlined/SettingOutlined/LinkOutlined/PictureOutlined)+ client.ts 5 fn + ResourceOption 类型。**偏差**:image-risk 的 ContainerImage 节点富化见 Phase 5.2;access-link 简化为从 Application 起点非 Ingress(设计 起于 Ingress);alert-aggregation 结构不同见下条 Phase 5.1;不做 config-impact 的 change-events 侧面板。6 视图进度 1/6 → 6/6(Phase 5.1 补齐 alert-aggregation)。cargo test --workspace(28 test result ok;engine-identity 25 含 views 8)+ clippy --workspace -D warnings + desktop check/clippy + 前端 tsc/vitest 20 + vite build 全绿。**运行时**:app X11 启动 OK(`GDK_BACKEND=x11 npm run tauri dev`,runtime ready / 2 connector / SQLite 载 78 真集群节点 / 5 view 命令注册无 panic / handler_executor env 开关 live);**GUI 像素渲染验证受阻于环境**(webview GPU 合成层,XGetImage 抓全黑;`WEBKIT_DISABLE_COMPOSITING_MODE=1` 无效;无 scrot/maim/xdotool),渲染走既有 Phase 2.7 已 GUI 验证的 `<TopologyView>` 组件,本改动只喂新 GraphResponse(subgraph 逻辑 8 单测 + 命令编译/类型检查覆盖)。真集群 SQLite 含 SCHEDULED_ON+USES 边,node-impact/config-impact 有真实数据。**用户视觉 smoke**(GNOME Wayland 会话,程序化截图全受阻:XWayland surface 抓黑 + D-Bus AccessDenied,装 xdotool/scrot 亦无效;改由用户肉眼确认):**node-impact 修一处 type 名 bug 后确认** —— 选择器查 `KubernetesNode` 但 Rust connector 产 `Node`,改 `resourceTypes={["Node"]}` 后 vm1/vm2/vm3 可选 + 爆炸半径渲染;config-impact(Secret/ConfigMap)+ access-link(Application)type 名正确;image-risk 空(connector 无 image 节点,已知 gap)
- [x] Phase 5.1:alert-aggregation 视图(补齐第 6 视图,6/6)。与其余 5 视图不同:起点不是拓扑节点,而是 AlertRegistry 里的 firing 告警。**engine-changes `alert_aggregation.rs`**(新模块,5 测试):`alert_aggregation_graph(&AlertRegistry, &Topology, severity, depth) -> GraphResponse` —— 过滤 firing(+severity),每个告警**合成** `AlertEvent` 图节点(type=AlertEvent,severity/status/metric 入 properties)+ 一条 `FIRED_ON` 边(alert→`resource_ref`,若在拓扑),再从 resource_ref 用既有 `engine_identity::views::subgraph` Both 方向 + 全边类型白名单展开邻域(同 resource 多告警只展开一次,节点/边去重)。查询时 join,**不改** AlertRegistry/Topology 模型(alert=L3 动态观测,topology=L2 结构,刻意分离)。复用 `summarize`。**desktop** `commands/views.rs::alert_aggregation`(clone registry 出锁再做图构建)+ generate_handler 注册。**前端** `AlertAggregationView`(无起点 Select,severity 过滤 + depth slider + 复用 TopologyView;显示 firing 告警数)+ page + 路由 + 菜单(AlertOutlined)+ client.ts `alertAggregation`。**偏差**:设计 forward 到 Application(1..4 跳);本 port Both 邻域(我们的边方向里 Pod 正向到不了 app,Both 跨 resource 类型更鲁棒);丢 4 个额外端点(metrics/inspection);无 live 源(告警需先录入)。cargo test --workspace + clippy --workspace -D warnings + desktop check/clippy + 前端 tsc/vitest 20 全绿
- [x] Phase 5.2:image 节点富化(让 image-risk 真集群非空)。**k8s mapper**(modules/connectors/k8s/src/mapper.rs):Container 循环里每个容器产 `ContainerImage` node(`image:{c}:{ns}:{image-ref}`,同镜像靠 resource_id 在 `facts_to_graph` 去重;attrs cluster/namespace/name/image)+ `USES_IMAGE[container→image]` 边(语义区别于 config 的 `USES`,对齐 USES_IMAGE 概念)。**engine-identity** `IMAGE_RISK_EDGES` 加 `USES_IMAGE`(设计 view5 用 plain USES,本 port 加 USES_IMAGE 偏差已记)。**realistic-fixture 测试**更新:加 ContainerImage + USES_IMAGE,image-risk 断言从空改非空(image→container→pod)。**前端** `SHAPE_BY_TYPE` 加 ContainerImage/AlertEvent/Application/ApplicationComponent 形状;ImageRiskPage emptyHint 更新(不再「预期空」)。**k8s mapper 测试**:`all_facts_carry_k8s_source` 计数更新(node 20→22 / edge 17→19)+ 新 `uses_image_edge_per_container`(2 container→2 image node + 2 USES_IMAGE 边)。image-risk 现可:image→container(USES_IMAGE 反向)→pod(RUNS 反向)→node(SCHEDULED_ON 反向)。k8s wasm 已重建。cargo test(engine-identity views 9 / k8s mapper 19)+ clippy(engine --workspace + k8s)+ 前端 tsc/vitest/build 全绿

### Phase 6 进展(技术债 pass,feature parity 之后)

> v0.2.0 里程碑(feature parity)后的架构债清理。C1 Identity v1 **deferred**(需第二数据源才有真实冲突可解,见下)。

| 增量 | 内容 | 状态 |
|---|---|---|
| C2 | **resource_type/edge_type 中央注册表**(根治 Node Impact 词表漂移)。`engine_core::types` 新模块:`resource_type`(16 const + ALL + is_known)+ `edge_type`(PRODUCED/KNOWN + is_known),wire-format `&str` const(非 enum,因值跨 serde/JSON/WIT)。4 校验测试含 `is_known("KubernetesNode")==false` 回归守卫 + const 名值对齐。替换 Phase 1 占位 `ResourceType` enum。迁移 7 个 host 侧生产消费点:engine-identity views.rs 4 白名单 + realistic-fixture、engine-changes PROPAGATION_EDGES / ALERT_CONTEXT_EDGES、engine-recovery action_defs 22 条 PropagationRule。**限制(文档化)**:modules(WASM guest)是独立 workspace 不依赖 engine-core,guest 输出 vs host registry 的漂移靠 realistic-fixture 测试 + `inspect_views` headless 工具检测。未来 codegen-from-WIT 单一源可闭合 guest/host 间隙 | ✅ |
| C3 | **Parquet 归档**(填三层数据契约 C 层命名缺口)。`engine-storage` 启用 `parquet` feature(parquet+arrow 59 optional)+ `parquet_store.rs`:`ParquetStorage`(append-only,按 `(date, source)` 分区 `{root}/dt={date}/{source}-{max_ts}.parquet`,一批一文件 data-lake 式;date 从 Fact.timestamp UTC 派生[Hinnant civil-from-days,无 chrono])+ `archive_batch`/`read_partition`/`read_all` + impl Storage(backend "parquet")+ 5 round-trip 测试。`StorageError` 加 Parquet/Io variant。复用 `FactBatch::to_record_batch` + `fact_schema` canonical Arrow 路径。example `archive_facts.rs`(SQLite latest facts 快照到 Parquet,反复跑累加成历史)+ `make engine-archive`。真集群验证:373 facts → 2 分区文件(dt=2026-07-12 + dt=2026-07-23,跨天正确拆分)156K。SQLite 仍存 latest,Parquet 存历史 | ✅ |
| C4 | **顶层 Makefile**(对齐 doc/16 §10)。修 bug:setup/dev-frontend 原指向已 rename 的 frontend/。加自文档 `help`(`##`+awk)+ 聚合 target(test-all/check-all/build-all)+ engine-cli-tick/tick-loop + modules 用 `cargo wasi-build` alias + modules-build-one + desktop-web + engine-dump-topology/inspect-views/archive-facts headless 工具 target。legacy 设计 target 加 `ref-` 前缀保留(oracle 仍可跑) | ✅ |
| C1 | **Identity Resolver v1**(correlation-key 合并,doc/11 §4-5 minimal)。**✅ Phase 8.2 完成**:code-repo `BUILDS`(repo→ContainerImage)是真冲突源 —— k8s `image:{c}:{ns}:{ref}` 与 code-repo `image-ref:<ref>` 两端对同一 image 用不同 key,经共享 `image-ref:<normalize(ref)>` correlation key 合并。`engine-identity/src/correlation.rs::rewrite_by_correlation_key` = `resolve()` 委托 `facts_to_graph` 前的 pre-rewrite pass(BFS 聚簇 + source-priority winner k8s>code-repo + lex-min tiebreak + 边端点/parent remap;attr 合并复用 `health_merge` canonical 序,决定性 → diff-stable)。correlation_keys 走 `attributes_json`(**零 schema 改**:WIT/Arrow/Parquet/SQLite 全不动)。`module-sdk::normalize_image_ref` 共享(砍 `@digest` / 补 `:latest` / 剥 `docker.io`)。8 单测 + resolve-level 集成测试证 BUILDS 挂上 k8s image → repo→image→container→pod 联通;真集群 tick 证两端对真实 otel-demo image 发同 key。**偏差**:minimal v1(无 provenance/confidence/`arbiter_decisions` 表/manual-queue/full `field-ownership.yaml` → 留 v2);仅 ContainerImage exercised;source-priority winner 非 confidence 仲裁;digest-pinned 合并留 v2 | ✅ |
| Connectors UI | **dogfood 驱动的 connector 运行时观测页**(补「sync 状态只能看日志」痛点)。痛点实证:`list_connectors` 只列**已加载** connector(禁用/加载失败不可见),后台 `sync_loop` 每 30s 跑 `sync_all` 但 per-connector 产出**被丢弃**(只手动 `sync_all_now` 返明细)。**实装**:`desktop commands/connectors.rs` — `ConnectorStatus`(静态 manifest 字段 name/kind/enabled/capabilities/sync_interval/config/fs_roots + 运行时 last_synced_at/fact_count/errors/duration_ms + `load_error`)+ `AppState.connector_statuses: tokio::sync::Mutex<Vec<ConnectorStatus>>`。启动 `seed_connector_statuses` 从**完整 manifest** 播种(禁用/失败模块也入表;`loaded`≠`enabled`:失败模块 enabled 仍 true 但 loaded=false 带 load_error)。`run_sync` 内 `update_connector_statuses` 刷新(**手动 sync_all_now + 后台 sync_loop 共用 run_sync → 一处更新覆盖两路**)。`get_connectors_status` 命令。engine-wasm `ConnectorSyncStatus` 加 `duration_ms`(之前只有 `total_duration_ms` 聚合,per-connector 计时被丢)。**前端** `ConnectorsPage`+`ConnectorsView`(主表 connectors + 次表 handlers;5s react-query 轮询让后台 loop 活动可见;status 三态徽章 running/failed/disabled;展开行全量 config/fs_roots/load_error/errors;「Sync all now」按钮)。MainLayout 菜单(ApiOutlined)+ `/connectors` 路由。**测试**:connectors.rs 4(seed disabled/loaded/failed + load_error + update fills runtime fields + leaves unsynced  untouched)+ wasm DTO duration_ms 断言;engine workspace test/clippy 全绿;前端 tsc/vitest 21。**偏差**:无 per-connector「只 sync 这一个」(scope;sync_all 够快,留后续);GUI 像素 smoke Wayland 受阻(走 tsc + 单测覆盖,肉眼确认留用户,同 Phase 5/6 既定) | ✅ |
| 观测补齐 | **按桌面工具比例给 app 自身补观测**(刻意不上全套 OTel —— 无第二个信号消费者,过度工程):① **日志落盘** — tracing-appender 按天滚动(stderr 双写)+ 启动清 >14 天旧文件(`SRE_GRAPH_LOG_DIR` 覆盖,默认 app data/logs/);此前日志只到 stdout,桌面用户侧不可调试。② **sync 管线 span 树** — engine `sync_all`/`run_sync`(connector 字段)`#[instrument]` + desktop `run_sync` 整体 + `persist_raw_facts`/`load_current_topology`/`diff`/`apply_changes` 四阶段 span(`Instrument::instrument` 包 future,不跨 await 持 guard);`FmtSpan::CLOSE` 开,细级别可见分段耗时。③ **connector 状态历史** — `ConnectorStatus.history` 12 轮环形采样(oldest→newest 截头)→ Connectors UI 展开行趋势条(纯 CSS,绿正常/红有错);快照→准时间序列。**测试**:desktop 19(历史截断 + 保留期边界[注入 now])+ engine-wasm 62 回归 + 前端 21 全绿。**留后续**:wasmtime fuel 给 guest 资源计量(第三方插件场景的安全闸,非当前需求);自观测 dogfood(需真实消费者才做) | ✅ |
### Phase 7 进展(PRD-004 connector 收官 —— 5/5)

> 第三条~第五条 connector(jaeger / k8s-events / flagd)。jaeger 补调用图谱维度;k8s-events + flagd 是**首批 ChangeEvent 生产者**(产 `kind="change"` Fact,desktop 路由到 `record_change`,不进 topology)。doc/11/13 的 PRD-005「trace → 未知依赖发现」全版 + C1 留 code-repo 源(见 Phase 6 C1 行)。

| 增量 | 内容 | 状态 |
|---|---|---|
| 7.1 | **jaeger trace connector**(`modules/connectors/jaeger`)。对照设计(jaeger/trace_aggregator 设计:GET `{jaeger_url}/api/services` + 逐服务 `/api/traces?service=&lookback=&limit=`,数**跨服务 `CHILD_OF`** span 引用聚合成 `CALLS` topology-edge Fact(端点 = k8s ApplicationComponent 节点 id)。**只产边不产节点**(设计 行为;节点由 k8s connector 建,无 comp 节点的边悬空被 `facts_to_graph` 过滤)。mapper 纯函数 host 可测(10 测试,移植 `TestTraceAggregator` 7 例 + 边形状 + null-references 容错 + service 过滤);`jaeger_http_e2e.rs`(mock 路由 /api/services vs /api/traces + deny-by-default)。**module-sdk 抽 `naming::{normalize_component_name, strip_release_prefix, component_id}`**(k8s + jaeger 共享 `comp:` id 派生,防漂移 —— C2 词表漂移同类教训);k8s mapper 改 import。`edge_type::CALLS` 入 registry(PRODUCED+KNOWN)+ `ACCESS_LINK_EDGES` 白名单(从 Application 沿调用链上下游)。manifest 走 desktop 托管 kubectl proxy 的 `/jaeger/ui` 前缀(otel-demo Helm quirk)+ threshold 2(真集群 trace 稀疏)。**真集群 headless**:engine-cli tick -> **11 条 CALLS 边**(frontend→ad/currency/product-catalog/recommendation,recommendation→flagd/product-catalog,frontend-proxy→frontend/imageprovider/flagd,frontend-web→frontend-proxy,loadgenerator→frontend-proxy;call_count 18–224),call 拓扑正确。**C1 仍 deferred**:trace 不产 novel 节点 → 无跨源合并冲突(见 Phase 6 C1 行)。**偏差**:不做 doc/13 §8 span-attr 外部依赖发现(本集群无 `peer.service`/`db.system`/`messaging.system`);无 delete-on-disappear(靠现有 sync diff/apply_change_set);edges-only | ✅ |
| 7.2 | **k8s-events connector + change-fact routing**(`modules/connectors/k8s-events` + `desktop wasm.rs::run_sync`)。对照设计(k8s_events 设计:poll `GET {api_base}/api/v1/namespaces/{ns}/events`,挑 `INTERESTING_REASONS`(`ScalingReplicaSet`/`SuccessfulRescale` → `deployment_rolled`),involvedObject(Deployment / ReplicaSet[砍 hash] / Pod)→ target resource_id,产 `kind="change"` Fact。**新基建**:run_sync 解 `kind="change"` Fact → `engine_changes::ChangeRequest` → `record_change` → upsert(与 detect_changes 同款;guest 自管 baseline/dedup 故不经 first_sync gate;topology-facts 流向不变,resolve 按 kind 过滤)。**有状态 guest**(新模式):首次 sync baseline 种 UID 不发,之后只发新 UID,状态存 `thread_local!`(WasmConnector 复用 instance,跨 sync 持久;前 3 connector 无状态)。mapper 9 测试(移植 `TestK8sEventConnector` 4 + 补充);`k8s_events_e2e`(call-counter mock,2-sync baseline→发 + deny-by-default)。**给「变更无 live 源」(Phase 3.6 偏差)补了 K8s 事件 live 源**。**偏差**:poll 非 watch(真 watch 需 WIT stream + WASIp3);只 `INTERESTING_REASONS` 契约(设计 `Killing`/`restartCount` 文档有实现无,不 port);真集群 smoke 阻于本会话集群 API server 不可达,graceful-failure 验证(下线 proxy → 0 fact + error 不 panic) | ✅ |
| 7.3 | **flagd connector + OTel Demo 8 scenario 表**(`modules/connectors/flagd`)。对照设计(flagd 设计 + otel_demo_scenarios 设计:POST `{flagd_url}/flagd.evaluation.v1.Service/ResolveAll`(Connect-RPC transcoded,JSON body `{}`)拿 flag 快照,diff 上一轮,每条翻转产 `configmap_updated` change-fact(target = flagd ConfigMap 节点),命中 **8 故障 scenario** 时富化 `diff_summary.scenario.{recommended_action,target_component,finding_severity,expected_metric}` + description 后缀 `[scenario=… 推荐动作=… 目标组件=…]`。**经 `http-write` capability**(Connect unary 走 POST,复用既有 write —— 无需新 WIT/host)。**有状态 guest**:首次 baseline 存快照不发,之后 diff。mapper 15 测试(移植 `TestFlagdConnector` 4 + scenario enrichment 2 + `TestScenarios` 完整性 + diff);`flagd_e2e`(POST mock,2-sync baseline→翻转 + scenario 富化 + http-write deny-by-default)。**偏差**:**本集群未部署 flagd**(无 svc/deploy/ConfigMap)→ 真集群不可达,仅 e2e/contract 验证(同 prometheus OOM);真值需部署 flagd。PRD-004 5/5 收官(k8s/prometheus/jaeger/k8s-events/flagd) | ✅ |

### Phase 8 进展(code-repo 源 —— PRD-006,图谱最后一维:代码层)

> doc/12 PRD-006 的桌面适配。doc/12 假设 SaaS(GitLab/GitHub Open API + webhook + PRD-005 Fact Bus);webhook 是桌面反模式(无 HTTP server,同 image_pushed)、云 API 违 data-stays-on-machine,故改 **本地 fs 扫描**。新 `fs-read` capability —— 首个非网络 capability(deny-by-default + path-root allow-list)。

| 增量 | 内容 | 状态 |
|---|---|---|
| 8.1 | **code-repo connector + fs-read capability**(`modules/connectors/code-repo` + `engine/crates/engine-wasm/src/fs_host.rs`)。`fs-read` WIT interface(read-file/read-dir,read-only,error variant)+ `connector-world` import(handler-world 不 import → 单 Host impl);`fs_host.rs` 纯函数(cap 查 + canonicalize + path-root allow-list **第一天强制**,防目录穿越 `../../etc/passwd` + 符号链接逃逸;read-only,区别于 raw WASI preopens;8 单测)+ `runtime.rs::FsReadHost for State` + `load_with_fs_roots`/`load_with_roots`;`ModuleManifest.fs_roots` + `from_manifest` canonicalize 透传。connector 扫本地仓库目录(Dockerfile `FROM`→`BUILDS`、package.json/go.mod/Cargo.toml/requirements.txt→`Library`+`DEPENDS_ON`),**无状态**(重扫幂等);mapper 13 测试(Dockerfile/npm/go/cargo/pypi 解析 + id/edge 形状)。registry 加 `CodeRepo`/`Library` + `BUILDS`/`DEPENDS_ON`。**零 pipeline 改**:topology-node/edge fact 自动走 resolve/apply。e2e 2(真 fs fixture + deny-by-default)。**偏差**:`BUILDS` v0 best-effort(repo 不知 cluster/ns → 悬空过滤);PR/MR 事件 OUT(webhook 反模式,手动入口同 image_pushed);无规则抽取(Sprint 2);非 GitLab/GitHub API(本地扫描)。**C1 留 8.2**:BUILDS 是真冲突源(image-ref correlation key),建合并机让 repo→image 挂上。gate 全绿(engine test+clippy / modules wasi-build+wasi-clippy / desktop check+clippy / 前端 tsc) | ✅ |
| 8.2 | **Identity Resolver v1 — correlation-key merge(C1 收官)**。`engine-identity/src/correlation.rs::rewrite_by_correlation_key` = `resolve()` 委托 `facts_to_graph` 前的 pre-rewrite pass:解 node fact attrs 的 `correlation_keys` → BFS 聚簇(支持传递合并)→ source-priority winner(k8s=10 > code-repo=5,lex-min tiebreak,决定性/顺序无关)→ 每簇产 1 合并 node fact(winner attrs + loser 补缺失 key,`Value::Object(BTreeMap).to_string()` canonical 序 → diff-stable)+ drop loser → remap edge source/target + node parent_resource_id。**8 单测**(merge/winner/remap/orphan/determinism/attr-merge/passthrough)+ resolve-level集成测试(k8s image + code-repo BUILDS+image-ref 同 key → BUILDS 挂上 k8s image 节点,repo→image→container→pod)。`module-sdk::normalize_image_ref` 共享(砍 `@digest`/补 `:latest`/剥 `docker.io`)。k8s ContainerImage attrs 加 `correlation_keys:["image-ref:<norm>"]`;code-repo 弃 v0 `image:{c}:{ns}:{ref}` byte-match,改发自产 `image-ref:<ref>` 节点(带同 key)+ BUILDS(repo→image-ref),resolver 合并后 remap 端点(永不悬空)。**零 schema 改**:correlation_keys 走 attributes_json(WIT/Arrow/Parquet/SQLite 不动);engine-core `facts_to_graph` 共享契约零改(无生产调用方绕过 resolve)。真集群 tick 证两端对真实 otel-demo image(`ghcr.io/open-telemetry/demo:1.11.0-<svc>`)发同 key。gate 全绿(engine test+clippy -D warnings[identity 33 含 8 correlation]/ modules wasi-build+wasi-clippy[code-repo 14 k8s 19 sdk 4]/ desktop check+clippy / 前端 tsc)。**偏差**:minimal v1(无 provenance/confidence/`arbiter_decisions`/manual-queue/full `field-ownership.yaml` → 留 v2);仅 ContainerImage exercised;digest-pinned 合并留 v2 | ✅ |

- [x] 真集群 headless smoke(2026-07-24):VM1+VM2 启动后 `engine-cli tick` 验证 —— k8s **304 fact**(141 节点 + 163 边,全 8 边类型 BELONGS_TO/DEPLOYED_AS/SCHEDULED_ON/RUNS/USES_IMAGE/USES/ROUTES_TO/EXPOSES)/ jaeger **CALLS 真边**(recommendation→flagd 54、→product-catalog 54、loadgenerator→frontend-proxy 7 等;count 随 trace volume 波动)/ code-repo **14 fact**(2 CodeRepo + 4 Library + 4 DEPENDS_ON + 4 BUILDS,otel-demo-shaped fixture)/ k8s-events **live emit**(emailservice+cartservice rollout → facts 319→327,~7 `deployment_rolled` change fact;**--loop 持久 runtime 才发**,单 tick baseline=0)。GUI 像素 smoke 仍 Wayland 受阻(低优,渲染走 Phase 2.7 已验 TopologyView)
- [x] **v0.4.0 发布**(2026-07-25):4 PRD(recovery/changes/reports/connectors)+ code-repo 源(Phase 8.1)+ C1 correlation-key 合并(Phase 8.2)= 完整 v2 story。tag at 8.2 commit。
- [x] **dogfood UX pass**(2026-07-25,真集群 GUI 会话浮现):TopologyView 加搜索 + 按类型展示过滤 + health/risk 可点击 legend 过滤(TopologyPage 统一一处,不再两套);记录变更/告警表单的 `target_resource_id`/`resource_ref` 改为从拓扑节点搜索选择(防手敲拼错 → 关联失败);移除死占位 "Phase 4+" 菜单组(Connectors/Fault-sim)。C1 BUILDS 挂接在真集群 SQLite 确认(repo→image→container→pod)。
- [x] **Connectors UI**(dogfood 浮现 → ✅ 落地):connector 管理页(健康/sync 状态/间隔/per-connector config/errors)。`desktop commands/connectors.rs` 持久状态注册表(manifest 全量播种 + run_sync 刷新覆盖手动/后台两路)+ `ConnectorsPage`/`ConnectorsView`(5s 轮询,status 三态徽章,展开行明细)。详见 Phase 6 「Connectors UI」行。
- [ ] 后续:**Unknown Dep Queue(doc/11 §6 / doc/13)DEFERRED** —— 实测真集群 otel-demo trace 100% 内部(jaeger span tags 只有 `net.peer.ip`(pod IP)/ `http.host=otel-demo-<svc>:port`/ `rpc.system=grpc`,无任何外部依赖),② 会检测到 0 个真 unknown(同 C1 当初 deferred 的合成数据陷阱)。需指向有真实外部依赖的集群(service 调 api.stripe.com / 云 API)或注入假外部依赖才能 demo。**buildable + clean**(jaeger tags 提取 → `kind="unknown-dep"` fact → run_sync 路由(镜像 change-fact)→ `unknown_deps` 表(克隆 change_events)→ UnknownDepRegistry → UnknownDepsView(克隆 AlertsView)→ promote ExternalSaaS 复用 correlation.rs merge);code-repo enrichment 需 Sprint 2(未建)。+ image_pushed 真 webhook(可能永久跳过)+ report 调度持久化优化 + flagd 真集群 smoke(需装 flagd)+ **C1 v2**(provenance/confidence/arbiter_decisions/manual-queue/full field-ownership.yaml)+ 真 GUI smoke(Wayland 程序化截图受阻;C1 merge 走 desktop sync,需肉眼确认 BUILDS 挂上 k8s image 渲染)

---

## 开发约定

- **代码分区**:`engine/`(Rust 内核)/ `desktop/`(Tauri)/ `modules/`(WASM connector)/ `specs/`(WIT)。**业务逻辑进 engine-core,desktop 只做薄命令包装**。
- **契约先行**:写新代码前对照三层数据契约(`doc/15`:WIT / Tauri commands / Arrow+SQLite+Parquet)。
- **WASM 安全**:connector 是不可信插件,只经 host 注入 capability(deny-by-default)访问外界;**不在 desktop 起 HTTP server**(进程内 IPC),**不在 WASM 直接 syscall**。
- **测试纪律**:领域逻辑写 I/O-free 纯函数(吃 `&Topology` / `&ChangeRegistry`,可单测);行为偏差在 commit msg 明示。
